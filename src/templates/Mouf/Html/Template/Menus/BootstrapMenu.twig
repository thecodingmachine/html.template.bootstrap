{% if (not hidden) and children %}
<ul class="nav {% if (navbar) %}navbar-nav{% endif %} {% if (tabs) %}nav-tabs{% endif %} {% if (tabs) %}nav-pills{% endif %}{% if (justified) %}nav-justified{% endif %}  {{ cssClass }}">
	{% for child in children %}
		{{ toHtml(child) }}
	{% endfor %}
</ul>
{% endif %}